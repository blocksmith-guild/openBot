
1. analyse the $file's content within its context
2. find the best name that would describe the file's content
3. format the name in a slug manner
4. make sure that name doesn't conflict with other files,
   rename the file using that canonical name.
5. AND verify the file has been moved correctly"
6. if it fails append a 'mv' line to a shell script 'pending-moves.sh and inform the user
